workflows:
  build-android-debug:
    name: Moodle App Build for Android (DEBUG - splash PNG preferencia)
    max_build_duration: 60

    environment:
      node: 20.18.0
      java: 17
      vars:
        PACKAGE_NAME: "com.upc.moodleapp"
        APP_NAME: "Moodle UPC"

    scripts:
      - name: Instalar dependencias
        script: |
          set -e
          npm install -g @ionic/cli cordova cordova-res gulp
          npm ci || npm install
          ionic build

      - name: Preparar recursos (icon/adaptive)
        script: |
          set -e
          [ -f resources/icon.png ] || { echo "❌ Falta resources/icon.png"; exit 1; }
          [ -f resources/android/logo.png ] || { echo "❌ Falta resources/android/logo.png"; exit 1; }

          # Adaptive icon para que cordova-res no falle
          mkdir -p resources/android
          cp resources/icon.png resources/android/icon-foreground.png
          cp resources/icon.png resources/android/icon-background.png

          ionic cordova resources android --force --icon || true
          echo "✅ cordova-res ejecutado."

      - name: Limpiar cachés/artefactos para forzar assets nuevos
        script: |
          set -e
          echo "🧹 Limpiando www y plataformas…"
          rm -rf www platforms/android || true

          echo "🧽 Borrando caches comunes de tooling…"
          rm -rf node_modules/.cache || true
          rm -rf ~/.gradle/caches/build-cache-* || true

          echo "🔎 Mostrando assets reemplazados:"
          ls -lah src/assets/img || true

      - name: Preparar plataforma Android (limpio)
        script: |
          set -e
          ionic cordova platform rm android || true
          ionic cordova platform add android

          # Verifica que el logo exista en el árbol Android (copiado por <resource-file>, opcional)
          test -f platforms/android/app/src/main/res/drawable-nodpi/logo.png \
            || echo "⚠️  No se encontró logo.png en res/ (no es crítico si la preferencia ya usa el archivo del proyecto)"

          echo "▶ Mostrando preferencia en config.xml (para auditar):"
          grep -n "AndroidWindowSplashScreenAnimatedIcon" config.xml || echo "⚠️  No se ve la preferencia en config.xml"

          echo "🧼 cordova clean android (fuerza recompilación completa)…"
          ionic cordova clean android || true

      - name: Compilar APK (DEBUG)
        script: |
          set -e
          ionic cordova build android --debug -- --packageType=apk

    artifacts:
      - platforms/android/app/build/outputs/apk/debug/*.apk
